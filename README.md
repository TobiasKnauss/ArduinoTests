# ArduinoTests

This repository contains test projects for various code elements and functionalities:
- data types: integers, float, enums, and others.
- functions: built-in and from libraries.
- technologies: concepts about various functionalities, e.g. working with classes, retrieving names from enum elements, communication over serial interfaces, running Arduino boards at low power consumption, and so on.
